# 应用视角的操作系统

## lab1:递归的非递归形式改写

- 改写前程序可简化为业务逻辑、递归调用组成，不同组合形式有不同改写方式，常采用0和1状态表示该栈帧内容是否启用业务逻辑进行改写

## lab2:最简单的hello_word

- C语言先进行_start调用main，再调用结束系统调用
- 最特殊的命令syscall，能改变程序之外的东西（包括唯一终止程序的办法）,前提是要把自身控制权完全交给操作系统
- 最简单的hello_word汇编程序

# 硬件视角的操作系统

## lab3：开机启动过程

# 程序和进程

## 进程管理

- 标准api：fork复制、execve重置、exit退出、spawn创建

## lab4：C程序创建子进程、打开程序

## 内存管理

- 什么是进程的地址空间？
- Linux（从低到高）: 代码段、数据段、bss段、堆、内存映射区、栈、vvar、vdso、共享库/动态链接库区域
- 内存的申请和回收的api：brk、sbrk、mmap、munmap

## lab5：查看linux中的程序的地址空间？ps/top、cat /proc/pid/maps文件

## lab6：C程序进程内存的申请和回收

## 操作系统文件对象

- UNIX：everything is a file，可用通用的文件工具组合处理，每个进程持有一个保存了该进程打开了的文件的数组
- windows：软件工程思想（数据结构接口）
- 例子：匿名管道/命名管道,一个读口文件，一个写口文件

## lab7：C程序可打开指定路径的文件，并进入while死循环，然后使用lsof命令行工具、及/proc/[pid]/fd观察进程如何持有打开的文件

## lab8：C程序新建命名管道，创建子进程，通过管道进行主子进程信息交互，并使用命令行工具观察进程如何通过文件持有管道的读写口